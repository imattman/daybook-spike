# as of Go 1.15 using `-h` results in exit status 0
exec dbk -h
! stdout .
stderr 'Usage of dbk:'

# confirm usage and non-zero exit with bad flag
! exec dbk --unknown-flag
! stdout .

# confirm some expected usage messaging
stderr 'Usage of dbk:'
stderr '-c'
stderr '-fields'
